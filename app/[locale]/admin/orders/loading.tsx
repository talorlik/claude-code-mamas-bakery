import { LoadingState } from "@/components/shared/states"

/**
 * Route-level loading UI for the admin order list while orders are fetched.
 */
export default function AdminOrdersLoading() {
  return <LoadingState />
}
