import { LoadingState } from "@/components/shared/states"

/**
 * Route-level loading UI for the admin product list while products are fetched.
 */
export default function AdminProductsLoading() {
  return <LoadingState />
}
